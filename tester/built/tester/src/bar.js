import { authMachine, lightMachine } from '../../client/src/machine/index.js';
const a = authMachine;
const p = lightMachine;
export default {
    bar: 'boo',
    authMachine: a,
    lightMachine: p,
};
